[package]
name = "unpack"
version = "0.1.0"
edition = "2021"
description = "Lettore degli archivi ARCH000 di The Binding of Isaac, con estrazione mirata"

[dependencies]
miniz_oxide = "0.9.1"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
test-support = { path = "../test-support" }
tempfile = "3"
