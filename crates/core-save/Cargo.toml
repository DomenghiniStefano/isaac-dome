[package]
name = "core-save"
version = "0.1.0"
edition = "2021"
description = "Lettore in sola lettura dei salvataggi di The Binding of Isaac: Repentance+"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
test-support = { path = "../test-support" }
