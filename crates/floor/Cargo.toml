[package]
name = "floor"
version = "0.1.0"
edition = "2021"
license.workspace = true
description = "The floor's grid and the game's documented secret-room placement rules"

[features]
test-api = []

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
floor = { path = ".", features = ["test-api"] }

[lints]
workspace = true
