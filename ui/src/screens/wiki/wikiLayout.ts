// The Wiki's list rows are drawn at the row-wide token; the virtualizer needs the same height as
// a number. The test beside this file keeps the two from drifting apart.
export const wikiRowHeight = 40
